# .github/workflows/reuse.yml
name: REUSE Compliance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  reuse:
    name: REUSE Compliance Check
    uses: ByronWilliamsCPA/.github/.github/workflows/python-reuse.yml@e753ebbf3c2fac40177495137391f2a45adab93a  # main 2026-05-07
    with:
      generate-spdx: true
      fail-on-missing: true
    permissions:
      contents: read
