# .github/workflows/scorecard.yml
name: OpenSSF Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 0 * * 0'

# Workflow-level permissions provide an audit surface; job-level block below is required by GitHub for reusable workflow inheritance.
permissions:
  security-events: write
  id-token: write
  contents: read
  actions: read

concurrency:
  group: scorecard-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scorecard:
    name: Scorecard Analysis
    uses: ByronWilliamsCPA/.github/.github/workflows/python-scorecard.yml@f05c26a424a708a73fc445a0ebb5b3ce476c1793  # main 2026-05-07
    with:
      publish-results: false
      upload-sarif: true
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
