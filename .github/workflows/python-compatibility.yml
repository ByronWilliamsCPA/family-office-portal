# .github/workflows/python-compatibility.yml
name: Python Compatibility

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  compatibility:
    name: Compatibility Matrix
    # Permissions are inherited from this workflow-level block by the reusable workflow call below.
    uses: ByronWilliamsCPA/.github/.github/workflows/python-compatibility.yml@e753ebbf3c2fac40177495137391f2a45adab93a  # main 2026-05-07
    with:
      python-versions: '["3.12"]'
      operating-systems: '["ubuntu-latest"]'
      include-windows: false
      include-macos: false
      source-directory: 'app'
      test-command: 'uv run pytest -v'
      coverage-report: true
      fail-fast: false
    permissions:
      contents: read
