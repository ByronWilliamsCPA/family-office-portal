# Compliance Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve all 38 findings (6 critical, 24 important, 8 suggested) from the 2026-05-07 repo-compliance audit of `ByronWilliamsCPA/family-office-portal`.

**Architecture:** Three sequential phases: Phase A fixes local files with no GitHub API dependency; Phase B scaffolds the missing `.github/workflows/` directory from org templates with project-specific adaptations; Phase C completes the remaining GitHub platform settings that require browser UI or gh CLI with admin access.

**Tech Stack:** GitHub Actions, uv, basedpyright, ruff, pytest, SonarCloud, Codecov, OSV-Scanner, Bandit, pip-audit, REUSE

---

## Phase A: Local File and Config Fixes

All tasks in Phase A can be run locally. Each task ends with a commit.

---

### Task 1: Move CODEOWNERS to `.github/CODEOWNERS`

**Findings resolved:** FOUND-004 (CODEOWNERS must live at `.github/CODEOWNERS`, not repo root)

**Files:**
- Delete: `CODEOWNERS` (repo root)
- Create: `.github/CODEOWNERS`

- [ ] **Step 1: Create `.github/` directory and write the new CODEOWNERS file**

```bash
mkdir -p .github
```

Create `.github/CODEOWNERS`:

```
# All files default to Byron Williams for review routing.
* @ByronWilliamsCPA
SECURITY.md @ByronWilliamsCPA
.claude/ @ByronWilliamsCPA
```

- [ ] **Step 2: Remove the root-level CODEOWNERS**

```bash
git rm CODEOWNERS
```

- [ ] **Step 3: Verify GitHub will read the new location**

```bash
cat .github/CODEOWNERS
# Expected output:
# # All files default to Byron Williams for review routing.
# * @ByronWilliamsCPA
# SECURITY.md @ByronWilliamsCPA
# .claude/ @ByronWilliamsCPA
```

- [ ] **Step 4: Stage and commit**

```bash
git add .github/CODEOWNERS
git commit -m "fix(compliance): move CODEOWNERS to .github/ (FOUND-004)"
```

---

### Task 2: Add `docs/compliance-reports/` to `.gitignore`

**Findings resolved:** FOUND-006 (compliance report artifacts must not be committed)

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: Confirm the entry is absent**

```bash
grep -c "compliance-reports" .gitignore
# Expected: 0
```

- [ ] **Step 2: Append the entry to `.gitignore`**

Open `.gitignore` and add the following block after the `# Project-specific` section (or at the end of the file):

```
# Compliance report artifacts (generated by /repo-compliance)
docs/compliance-reports/
```

- [ ] **Step 3: Verify**

```bash
grep "compliance-reports" .gitignore
# Expected: docs/compliance-reports/
```

- [ ] **Step 4: Commit**

```bash
git add .gitignore
git commit -m "fix(compliance): add docs/compliance-reports/ to .gitignore (FOUND-006)"
```

---

### Task 3: Add `authors` and `darglint` to `pyproject.toml`

**Findings resolved:** FOUND-007 (authors field required), TOOL-007 (darglint required in dev deps)

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: Add `authors` under `[project]`**

In `pyproject.toml`, after the `description` line, add:

```toml
authors = [
    {name = "Byron Williams", email = "byronawilliams@gmail.com"},
]
```

The `[project]` section should now read:

```toml
[project]
name = "family-office-portal"
version = "0.1.0"
description = "Secure family estate portal: aggregates financial and entity data from backend services, rendered behind Cloudflare Zero Trust."
authors = [
    {name = "Byron Williams", email = "byronawilliams@gmail.com"},
]
requires-python = ">=3.12"
```

- [ ] **Step 2: Add `darglint` to `[project.optional-dependencies] dev`**

In the `dev` list, append `"darglint>=1.8"`:

```toml
[project.optional-dependencies]
dev = [
    "basedpyright>=1.18",
    "ruff>=0.4",
    "pip-audit>=2.7",
    "darglint>=1.8",
    "interrogate>=1.7",
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "anyio[trio]>=4.4",
]
```

- [ ] **Step 3: Sync the lock file**

```bash
uv sync --extra dev
```

- [ ] **Step 4: Verify darglint is installed**

```bash
uv run darglint --version
# Expected: darglint 1.8.x
```

- [ ] **Step 5: Verify authors field**

```bash
python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); print(d['project']['authors'])"
# Expected: [{'name': 'Byron Williams', 'email': 'byronawilliams@gmail.com'}]
```

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml uv.lock
git commit -m "fix(compliance): add authors field and darglint dev dependency (FOUND-007, TOOL-007)"
```

---

### Task 4: Add `darglint` hook to `.pre-commit-config.yaml`

**Findings resolved:** PC-006 (darglint hook required)

**Files:**
- Modify: `.pre-commit-config.yaml`

- [ ] **Step 1: Append the darglint hook block**

Add the following repo entry to `.pre-commit-config.yaml`. The SHA `abc26b768cd7135d848223ba53f68323593c33d5` is the HEAD of `terrencepreilly/darglint` as of 2026-05-07.

```yaml
  - repo: https://github.com/terrencepreilly/darglint
    rev: "abc26b768cd7135d848223ba53f68323593c33d5"  # v1.8.1  # pragma: allowlist secret
    hooks:
      - id: darglint
        args: [-s, google]
```

Place this block after the `interrogate` hook block and before the `commitizen` block.

- [ ] **Step 2: Run pre-commit to verify the hook installs cleanly**

```bash
pre-commit run darglint --all-files
# Expected: darglint.....Passed (or warnings on undocumented functions; not a failure)
```

- [ ] **Step 3: Commit**

```bash
git add .pre-commit-config.yaml
git commit -m "fix(compliance): add darglint pre-commit hook (PC-006)"
```

---

### Task 5: Update `SECURITY.md` to reference GitHub Private Vulnerability Reporting

**Findings resolved:** OSSF-002 (CRITICAL - SECURITY.md must direct reporters to GitHub Security Advisories, not a public email address)

**Files:**
- Modify: `SECURITY.md`

- [ ] **Step 1: Replace the Reporting section**

Find and replace the "Reporting a Vulnerability" section. The current content:

```markdown
## Reporting a Vulnerability

Do not open a public GitHub issue for security vulnerabilities.

To report a security issue, email byronawilliams@gmail.com with:

- A description of the vulnerability
- Steps to reproduce or a proof-of-concept
- The potential impact (confidentiality, integrity, availability)
- Any suggested mitigations

You will receive an acknowledgment within 72 hours and a resolution update
within 14 days.
```

Replace with:

```markdown
## Reporting a Vulnerability

Do not open a public GitHub issue for security vulnerabilities.

**Use GitHub Private Vulnerability Reporting (PVR):**
[https://github.com/ByronWilliamsCPA/family-office-portal/security/advisories/new](https://github.com/ByronWilliamsCPA/family-office-portal/security/advisories/new)

Include in your report:

- A description of the vulnerability
- Steps to reproduce or a proof-of-concept
- The potential impact (confidentiality, integrity, availability)
- Any suggested mitigations

You will receive an acknowledgment within 72 hours and a resolution update
within 14 days.
```

- [ ] **Step 2: Verify the change**

```bash
grep -c "advisories/new" SECURITY.md
# Expected: 1
grep -c "byronawilliams@gmail.com" SECURITY.md
# Expected: 0
```

- [ ] **Step 3: Commit**

```bash
git add SECURITY.md
git commit -m "fix(compliance): use GitHub PVR in SECURITY.md instead of email (OSSF-002)"
```

---

### Task 6: Create `CHANGELOG.md`

**Findings resolved:** FOUND-009 (CHANGELOG.md required per OpenSSF baseline)

**Files:**
- Create: `CHANGELOG.md`

- [ ] **Step 1: Create `CHANGELOG.md`**

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Initial project scaffold: FastAPI application with Cloudflare Zero Trust auth
- Pre-commit hooks: ruff, basedpyright, bandit, detect-secrets, interrogate, commitizen, yamllint, markdownlint, no-em-dash
- Foundation files: README, LICENSE, SECURITY, CODEOWNERS, CLAUDE.md
- pyproject.toml with uv dependency management and PyStrict-aligned Ruff config
```

- [ ] **Step 2: Verify**

```bash
head -5 CHANGELOG.md
# Expected: # Changelog
```

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: add initial CHANGELOG.md (FOUND-009)"
```

---

### Task 7: Add SPDX identifier to `LICENSE`

**Findings resolved:** FOUND-013 (LICENSE must include SPDX identifier for machine-readable license detection)

**Files:**
- Modify: `LICENSE`

- [ ] **Step 1: Add SPDX identifier as first line**

The current `LICENSE` file starts with `MIT License`. Prepend an SPDX header:

```
SPDX-License-Identifier: MIT

MIT License
...
```

- [ ] **Step 2: Verify**

```bash
head -2 LICENSE
# Expected:
# SPDX-License-Identifier: MIT
#
```

- [ ] **Step 3: Commit**

```bash
git add LICENSE
git commit -m "fix(compliance): add SPDX-License-Identifier to LICENSE (FOUND-013)"
```

---

### Task 8: Create `docs/known-vulnerabilities.md`

**Findings resolved:** OpenSSF CVE documentation baseline (required before any release gate)

**Files:**
- Create: `docs/known-vulnerabilities.md`

- [ ] **Step 1: Create the file**

```markdown
# Known Vulnerabilities

This file documents CVEs and dependency vulnerabilities that cannot be immediately
resolved. Each entry must be reviewed quarterly. No entry may age past 60 days
without reassessment. The OpenSSF release gate blocks releases for any vulnerability
older than 60 days regardless of reassessment status.

## Template

| Field | Value |
|-------|-------|
| CVE | CVE-YYYY-NNNNN |
| Package | package-name@version |
| Severity | CRITICAL / HIGH / MEDIUM / LOW |
| CVSS | 0.0 |
| Affected versions | <constraint> |
| Fixed in | version or "no fix available" |
| First detected | YYYY-MM-DD |
| Reassessment due | YYYY-MM-DD (60 days from first detected) |
| Mitigation | Description of compensating control or reason deferral is safe |
| Tracking | Link to upstream issue or PR |

## Active Entries

_No known vulnerabilities at this time. Run `uv run pip-audit` to check._

## Resolved Entries

_None yet._
```

- [ ] **Step 2: Verify**

```bash
grep -c "Active Entries" docs/known-vulnerabilities.md
# Expected: 1
```

- [ ] **Step 3: Commit**

```bash
git add docs/known-vulnerabilities.md
git commit -m "docs: add known-vulnerabilities tracking file (OpenSSF baseline)"
```

---

### Task 9: Create `AGENTS.md`

**Findings resolved:** CLAUDE-006 (AGENTS.md required in repo root)

**Files:**
- Create: `AGENTS.md`

- [ ] **Step 1: Create `AGENTS.md`**

```markdown
# AGENTS.md

Agentic coding assistants (Codex, GitHub Copilot Workspace, and similar tools)
should read this file before beginning any task in this repository.

## Project overview

`family-office-portal` is a FastAPI web application that aggregates financial
and entity data from internal backend services. It runs behind Cloudflare Zero
Trust and serves a single authenticated family as a read-only portal.

## Source layout

```text
app/           Python package (all application code)
tests/         pytest test suite
docs/          Documentation and ADRs
.github/       CI/CD workflows and GitHub configuration
pyproject.toml uv project manifest and tool configuration
```

## Key conventions

- **Language**: Python 3.12+, no 3.10/3.11 compatibility shims
- **Package manager**: uv (not pip, not Poetry)
- **Type checker**: basedpyright strict mode (not mypy)
- **Linter/formatter**: ruff
- **Test framework**: pytest with pytest-asyncio and pytest-cov
- **Source root**: `app/` (not `src/`)
- **Commits**: conventional commits (`feat:`, `fix:`, `docs:`, etc.)
- **Pre-commit**: always run `pre-commit run --all-files` before committing

## What NOT to do

- Do not create `.py` files at the repo root; all Python belongs under `app/`
- Do not use `import mypy` or reference mypy configuration; use basedpyright
- Do not add `# type: ignore` or `# noqa` without a tracking reference
- Do not use `poetry` or `pip install` commands; use `uv sync`

## Global standards

Global agent and skill conventions live at `~/.claude/CLAUDE.md`.
Project-specific rules are in `CLAUDE.md` at the repo root.
```

- [ ] **Step 2: Verify**

```bash
head -3 AGENTS.md
# Expected: # AGENTS.md
```

- [ ] **Step 3: Commit**

```bash
git add AGENTS.md
git commit -m "docs: add AGENTS.md for agentic assistant context (CLAUDE-006)"
```

---

### Task 10: Migrate ADRs to `docs/architecture/adr/`

**Findings resolved:** FOUND-012 (architecture docs must be at `docs/architecture/`, not `docs/planning/adr/`)

**Files:**
- Create dir: `docs/architecture/adr/`
- Move: `docs/planning/adr/*.md` to `docs/architecture/adr/`

- [ ] **Step 1: Create target directory and move files**

```bash
mkdir -p docs/architecture/adr
git mv docs/planning/adr/adr-001-frontend-rendering-architecture.md docs/architecture/adr/adr-001-frontend-rendering-architecture.md
git mv docs/planning/adr/adr-002-authentication-cloudflare-zero-trust.md docs/architecture/adr/adr-002-authentication-cloudflare-zero-trust.md
git mv docs/planning/adr/adr-003-backend-data-aggregation.md docs/architecture/adr/adr-003-backend-data-aggregation.md
```

- [ ] **Step 2: Verify**

```bash
ls docs/architecture/adr/
# Expected:
# adr-001-frontend-rendering-architecture.md
# adr-002-authentication-cloudflare-zero-trust.md
# adr-003-backend-data-aggregation.md

ls docs/planning/adr/ 2>/dev/null && echo "OLD DIR STILL EXISTS" || echo "Old dir clean"
# Expected: Old dir clean
```

- [ ] **Step 3: Update any internal references**

```bash
grep -r "docs/planning/adr" . --include="*.md" -l
# If any files appear, open them and update the paths to docs/architecture/adr
```

- [ ] **Step 4: Commit**

```bash
git add docs/architecture/ docs/planning/
git commit -m "refactor(docs): migrate ADRs from docs/planning/adr/ to docs/architecture/adr/ (FOUND-012)"
```

---

### Task 11: Create `renovate.json`

**Findings resolved:** OSSF dependency update automation (Dependabot/Renovate required for supply chain health)

**Files:**
- Create: `renovate.json`

- [ ] **Step 1: Create `renovate.json`**

```json
{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "matchManagers": ["github-actions"],
      "pinDigests": true,
      "automerge": false
    },
    {
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    }
  ],
  "prConcurrentLimit": 5,
  "schedule": ["before 6am on Monday"]
}
```

- [ ] **Step 2: Verify JSON is valid**

```bash
python3 -m json.tool renovate.json > /dev/null && echo "Valid JSON"
# Expected: Valid JSON
```

- [ ] **Step 3: Commit**

```bash
git add renovate.json
git commit -m "chore: add renovate.json for automated dependency updates (OSSF supply chain)"
```

---

## Phase B: CI/CD Scaffold

All tasks in Phase B create workflow files under `.github/workflows/`. The org-level
workflow templates live at `/home/byron/dev/.github/workflow-templates/` and are the
authoritative source; the versions here are adapted for this project's conventions
(`app/` not `src/`, basedpyright not mypy, Python 3.12+ only, uv not Poetry).

---

### Task 12: Create `.github/workflows/ci.yml`

**Findings resolved:** CI-001, CI-002, CI-003, CI-004, CI-005, CI-006, CI-014 (CI pipeline required; must include ci-gate job named "CI Gate")

**Source template:** `/home/byron/dev/.github/workflow-templates/python-ci.yml`

**Adaptations from template:**
- Matrix: `["3.12", "3.13"]` only (project requires Python 3.12+)
- Type checker: `basedpyright` replaces `mypy`
- Source root: `app/` replaces `src/`
- Remove `scripts/check_type_hints.py` step (not present in this project)

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: Create `.github/workflows/` directory**

```bash
mkdir -p .github/workflows
```

- [ ] **Step 2: Write `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - develop
      - 'feature/**'
  push:
    branches:
      - main
      - develop

permissions:
  contents: read
  pull-requests: write
  checks: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CI_ENVIRONMENT: true
  UV_CACHE_DIR: ~/.cache/uv

jobs:
  setup-optimized:
    name: Setup with UV Caching
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      uv-hash: ${{ steps.cache-key.outputs.uv-hash }}
    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Generate cache keys
        id: cache-key
        run: |
          echo "uv-hash=${{ hashFiles('**/uv.lock') }}" >> $GITHUB_OUTPUT

      - name: Set up Python 3.12
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'

      - name: Install UV
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Cache UV dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5
        with:
          path: |
            .venv
            ~/.cache/uv
          key: py-deps-${{ runner.os }}-${{ steps.cache-key.outputs.uv-hash }}
          restore-keys: |
            py-deps-${{ runner.os }}-

      - name: Install dependencies
        run: uv sync --no-interaction --all-extras

      - name: Dependency validation
        run: |
          uv run python -c "
          import sys
          required_deps = ['fastapi', 'structlog']
          for dep in required_deps:
              try:
                  mod = __import__(dep)
                  version = getattr(mod, '__version__', 'unknown')
                  print(f'  OK {dep} ({version})')
              except ImportError as e:
                  print(f'  FAIL {dep}: {e}')
                  sys.exit(1)
          print('All core dependencies validated')
          "

  test:
    name: Test Suite (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: setup-optimized
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]
      fail-fast: false

    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install UV
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Restore dependency cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5
        with:
          path: |
            .venv
            ~/.cache/uv
          key: py-deps-${{ runner.os }}-${{ needs.setup-optimized.outputs.uv-hash }}
          restore-keys: |
            py-deps-${{ runner.os }}-

      - name: Install dependencies
        run: uv sync --no-interaction --all-extras

      - name: Run test suite
        run: |
          uv run pytest -v \
            --cov=app \
            --cov-report=xml:coverage.xml \
            --cov-report=term-missing \
            --cov-report=html:htmlcov \
            --cov-fail-under=80 \
            --junitxml=junit.xml \
            -o junit_family=xunit2 \
            --tb=short \
            --maxfail=10 \
            tests/
        env:
          CI_ENVIRONMENT: true

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: test-results-${{ matrix.python-version }}
          path: |
            junit.xml
            coverage.xml
            htmlcov/

  quality-checks:
    name: Quality Checks
    runs-on: ubuntu-latest
    timeout-minutes: 12
    needs: setup-optimized
    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python 3.12
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'

      - name: Install UV
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Restore dependency cache
        uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae  # v5.0.5
        with:
          path: |
            .venv
            ~/.cache/uv
          key: py-deps-${{ runner.os }}-${{ needs.setup-optimized.outputs.uv-hash }}
          restore-keys: |
            py-deps-${{ runner.os }}-

      - name: Install dependencies
        run: uv sync --no-interaction --all-extras

      - name: Type checking with BasedPyright
        run: uv run basedpyright app

      - name: Code formatting validation
        run: uv run ruff format --check

      - name: Linting with enhanced rules
        run: uv run ruff check . --config=pyproject.toml

  ci-gate:
    name: CI Gate
    runs-on: ubuntu-latest
    needs: [setup-optimized, test, quality-checks]
    if: always()
    steps:
      - name: CI Gate
        run: |
          if [[ "${{ needs.setup-optimized.result }}" == "success" && \
                "${{ needs.test.result }}" == "success" && \
                "${{ needs.quality-checks.result }}" == "success" ]]; then
            echo "CI Gate - Production Ready"
          else
            echo "CI Gate - Quality Standards Not Met"
            echo "setup-optimized: ${{ needs.setup-optimized.result }}"
            echo "test: ${{ needs.test.result }}"
            echo "quality-checks: ${{ needs.quality-checks.result }}"
            exit 1
          fi
```

- [ ] **Step 3: Verify YAML is syntactically valid**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "Valid YAML"
# Expected: Valid YAML
```

- [ ] **Step 4: Confirm ci-gate job name matches required status check**

```bash
grep 'name: CI Gate' .github/workflows/ci.yml
# Expected: (line with)    name: CI Gate
```

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add CI workflow adapted from org template (CI-001--CI-006, CI-014)"
```

---

### Task 13: Create `.github/workflows/security-analysis.yml`

**Findings resolved:** CI-015 (security-gate job required), CI-008 through CI-013 (security scanning tools)

**Source template:** `/home/byron/dev/.github/workflow-templates/python-security-analysis.yml`

**Adaptations from template:**
- Fix YAML bug: template has two jobs both named `security-gate` - keep the first full one, remove the stub
- Change dependency filter from `poetry.lock` to `uv.lock`
- Replace `snok/install-poetry` with `uv` setup pattern
- Replace `safety check` with `pip-audit`
- Remove `poetry export` step
- Change OSV scanner lockfile from `poetry.lock` to `uv.lock`

**Files:**
- Create: `.github/workflows/security-analysis.yml`

- [ ] **Step 1: Write `.github/workflows/security-analysis.yml`**

```yaml
name: Security Analysis

on:
  pull_request:
    branches:
      - main
      - develop
      - 'feature/**'
  schedule:
    - cron: '30 2 * * 1'  # Weekly Monday 2:30 AM UTC
  workflow_dispatch:

permissions: read-all

jobs:
  detect-changes:
    name: Detect Security-Relevant Changes
    runs-on: ubuntu-latest
    outputs:
      security_files: ${{ steps.filter.outputs.security }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Check for security-relevant file changes
        id: filter
        uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d  # v4.0.1
        with:
          filters: |
            security:
              - '**/*.py'
              - '.github/workflows/security-analysis.yml'
              - 'pyproject.toml'
              - 'uv.lock'

  codeql-analysis:
    name: CodeQL Security Analysis
    runs-on: ubuntu-latest
    needs: detect-changes
    timeout-minutes: 20
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Setup Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'

      - name: Install UV
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --only-group dev --no-interaction

      - name: Initialize CodeQL
        uses: github/codeql-action/init@95e58e9a2cdfd71adc6e0353d5c52f41a045d225  # v4.35.2
        with:
          languages: python
          queries: security-extended,security-and-quality
          config: |
            paths:
              - app
            paths-ignore:
              - tests

      - name: Build CodeQL Database
        uses: github/codeql-action/autobuild@95e58e9a2cdfd71adc6e0353d5c52f41a045d225  # v4.35.2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@95e58e9a2cdfd71adc6e0353d5c52f41a045d225  # v4.35.2
        with:
          category: "/language:python"
          upload: true

  dependency-security:
    name: Dependency Security Review
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      github.event_name == 'pull_request' &&
      needs.detect-changes.outputs.security_files == 'true'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Dependency Review
        uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48  # v4.9.0
        with:
          fail-on-severity: moderate
          license-check: true
          comment-summary-in-pr: always
          deny-licenses: GPL-2.0, GPL-3.0

  security-scanning:
    name: Multi-Tool Security Scan
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.security_files == 'true'
    timeout-minutes: 15
    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'

      - name: Install UV
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install Dependencies
        run: uv sync --all-extras --no-interaction

      - name: Bandit Security Analysis
        run: |
          uv run bandit -r app \
            -f json -o bandit-report.json \
            -c pyproject.toml || true
          uv run bandit -r app -c pyproject.toml || true

      - name: pip-audit Vulnerability Scan
        run: |
          uv run pip-audit \
            --format json \
            --output pip-audit-report.json || true
          uv run pip-audit || true

      - name: Upload Security Reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: security-scan-reports
          path: |
            bandit-report.json
            pip-audit-report.json

  osv-scanner:
    name: OSV Vulnerability Scanner
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.security_files == 'true'
    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Run OSV Scanner
        uses: google/osv-scanner-action/osv-scanner-action@c51854704019a247608d928f370c98740469d4b5  # v2.3.5
        with:
          scan-args: |-
            --lockfile=uv.lock
            --format=json
            --output=osv-results.json

      - name: Check OSV Results for HIGH/CRITICAL
        run: |
          if [ ! -f osv-results.json ] || [ ! -s osv-results.json ]; then
            echo "ERROR: OSV results file missing or empty"
            exit 1
          fi
          python3 << 'EOF'
          import json, sys

          def extract_severity(vuln):
              for field in ['severity', ('database_specific', 'severity')]:
                  if isinstance(field, tuple):
                      val = vuln.get(field[0], {}).get(field[1])
                  else:
                      val = vuln.get(field)
                  if val:
                      s = str(val).upper()
                      return 'MEDIUM' if s == 'MODERATE' else s
              return 'UNKNOWN'

          with open('osv-results.json') as f:
              results = json.load(f)
          high_crit = []
          for group in results.get('results', []):
              for pkg in group.get('packages', []):
                  for vuln in pkg.get('vulnerabilities', []):
                      sev = extract_severity(vuln)
                      if sev in ('HIGH', 'CRITICAL'):
                          high_crit.append(f"{vuln.get('id', 'UNKNOWN')} ({sev})")
          if high_crit:
              print(f"FAIL: {len(high_crit)} HIGH/CRITICAL vulnerabilities found")
              for v in high_crit[:5]:
                  print(f"  - {v}")
              sys.exit(1)
          print("PASS: No HIGH/CRITICAL vulnerabilities found")
          EOF

      - name: Upload OSV Scanner Report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: osv-scanner-results
          path: osv-results.json
          if-no-files-found: warn

  owasp-dependency-check:
    name: OWASP Dependency Check
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.security_files == 'true'
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Run OWASP Dependency-Check
        uses: dependency-check/Dependency-Check_Action@75ba02d6183445fe0761d26e836bde58b1560600  # v1.1.0
        with:
          project: 'family-office-portal'
          path: '.'
          format: 'ALL'
          args: >
            --enableRetired
            --enableExperimental
            --failOnCVSS 7

      - name: Upload OWASP Report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: owasp-dependency-check-report
          path: reports/
          if-no-files-found: warn

      - name: Upload SARIF to GitHub Security tab
        if: always()
        uses: github/codeql-action/upload-sarif@95e58e9a2cdfd71adc6e0353d5c52f41a045d225  # v4.35.2
        with:
          sarif_file: reports/dependency-check-report.sarif

  security-gate:
    name: Security Gate Validation
    runs-on: ubuntu-latest
    needs:
      - detect-changes
      - codeql-analysis
      - dependency-security
      - security-scanning
      - osv-scanner
      - owasp-dependency-check
    if: always()
    steps:
      - name: Validate Security Results
        run: |
          is_acceptable() { [[ "$1" == "success" || "$1" == "skipped" ]]; }
          if is_acceptable "${{ needs.codeql-analysis.result }}" && \
             is_acceptable "${{ needs.dependency-security.result }}" && \
             is_acceptable "${{ needs.security-scanning.result }}" && \
             is_acceptable "${{ needs.osv-scanner.result }}" && \
             is_acceptable "${{ needs.owasp-dependency-check.result }}"; then
            echo "Security gate passed"
          else
            echo "Security gate FAILED"
            echo "codeql: ${{ needs.codeql-analysis.result }}"
            echo "dep-security: ${{ needs.dependency-security.result }}"
            echo "scanning: ${{ needs.security-scanning.result }}"
            echo "osv: ${{ needs.osv-scanner.result }}"
            echo "owasp: ${{ needs.owasp-dependency-check.result }}"
            exit 1
          fi
```

- [ ] **Step 2: Verify YAML is syntactically valid and has exactly one security-gate job**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/security-analysis.yml'))" && echo "Valid YAML"
grep -c "name: Security Gate Validation" .github/workflows/security-analysis.yml
# Expected: Valid YAML
# Expected: 1
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/security-analysis.yml
git commit -m "ci: add security-analysis workflow (CI-008--CI-013, CI-015)"
```

---

### Task 14: Create `.github/workflows/pr-validation.yml`

**Findings resolved:** CI-016 (PR validation job must be named "Dependency & Standards Validation")

**Source template:** `/home/byron/dev/.github/workflow-templates/python-pr-validation.yml`

**Adaptations from template:**
- Rename job `name:` from `"PR Validation"` to `"Dependency & Standards Validation"`
- Change `source-directory: 'src'` to `source-directory: 'app'`

**Files:**
- Create: `.github/workflows/pr-validation.yml`

- [ ] **Step 1: Write `.github/workflows/pr-validation.yml`**

```yaml
name: PR Validation

on:
  pull_request:
    branches:
      - main
      - develop
    types:
      - opened
      - synchronize
      - reopened
      - edited

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  pr-validation:
    name: Dependency & Standards Validation
    uses: ByronWilliamsCPA/.github/.github/workflows/python-pr-validation.yml@main
    with:
      python-version: '3.12'
      source-directory: 'app'
      test-directory: 'tests'
      require-tests: true
      require-conventional-commits: true
      require-description: true
      min-description-length: 50
      check-breaking-changes: true
      add-size-labels: true
      coverage-threshold: 80
    permissions:
      contents: read
      pull-requests: write
      checks: write
```

- [ ] **Step 2: Verify job name**

```bash
grep "name: Dependency" .github/workflows/pr-validation.yml
# Expected: (line with)    name: Dependency & Standards Validation
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/pr-validation.yml
git commit -m "ci: add pr-validation workflow with correct job name (CI-016)"
```

---

### Task 15: Create SonarCloud workflow and properties

**Findings resolved:** CI-007 (quality gate must not use continue-on-error), CI-017 (SonarCloud workflow required)

**Source template:** `/home/byron/dev/.github/workflow-templates/python-sonarcloud.yml`

**Adaptations from template:**
- Replace `{{ cookiecutter.sonarcloud_organization }}` with `byronwilliamscpa`
- Replace `{{ cookiecutter.github_org_or_user }}_python-project` with `ByronWilliamsCPA_family-office-portal`
- SHA-pin `actions/checkout@v4`, `actions/setup-python@v5`, `SonarSource/sonarqube-scan-action@v4`, `sonarsource/sonarqube-quality-gate-action@master`, `actions/upload-artifact@v4`
- Remove `continue-on-error: true` from tests step AND quality gate step (CI-007)
- Change `--cov=src/python-project` to `--cov=app`
- Replace manual UV install with `astral-sh/setup-uv` action

**Files:**
- Create: `.github/workflows/sonarcloud.yml`
- Create: `sonar-project.properties`

- [ ] **Step 1: Write `.github/workflows/sonarcloud.yml`**

```yaml
name: SonarCloud

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'

      - name: Install UV
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --frozen --all-extras

      - name: Run tests with coverage
        run: |
          uv run pytest \
            --cov=app \
            --cov-report=xml:coverage.xml \
            --cov-report=term \
            --cov-branch \
            -v

      - name: Verify coverage report
        run: |
          if [ ! -f coverage.xml ]; then
            echo "::warning::Coverage report not found."
            echo '<?xml version="1.0" ?><coverage version="1.0"></coverage>' > coverage.xml
          fi

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@689fb39b34b9aa95ebc5f8f119343ddd51542402  # v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=byronwilliamscpa
            -Dsonar.projectKey=ByronWilliamsCPA_family-office-portal
            -Dsonar.python.version=3.12

      - name: Check Quality Gate
        uses: sonarsource/sonarqube-quality-gate-action@8e9b0ca0a7273d6f16986388d98393efdfcf56fd  # master
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Upload coverage artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: coverage-sonarcloud
          path: coverage.xml
          retention-days: 7
```

- [ ] **Step 2: Write `sonar-project.properties`**

```properties
sonar.projectKey=ByronWilliamsCPA_family-office-portal
sonar.organization=byronwilliamscpa
sonar.projectName=family-office-portal
sonar.sources=app
sonar.tests=tests
sonar.python.version=3.12
sonar.python.coverage.reportPaths=coverage.xml
sonar.exclusions=**/__pycache__/**,**/*.pyc
```

- [ ] **Step 3: Verify no cookiecutter variables remain and no continue-on-error**

```bash
grep -c "cookiecutter" .github/workflows/sonarcloud.yml
# Expected: 0

grep -c "continue-on-error" .github/workflows/sonarcloud.yml
# Expected: 0

python3 -c "import yaml; yaml.safe_load(open('.github/workflows/sonarcloud.yml'))" && echo "Valid YAML"
# Expected: Valid YAML
```

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/sonarcloud.yml sonar-project.properties
git commit -m "ci: add SonarCloud workflow SHA-pinned with no quality-gate bypass (CI-007, CI-017)"
```

---

### Task 16: Create `.github/workflows/coverage.yml` and `.codecov.yml`

**Findings resolved:** CI-018 (Codecov coverage upload required)

**Source template:** `/home/byron/dev/.github/workflow-templates/python-codecov.yml`

**Adaptations from template:**
- Restrict to Python versions used in this project: 3.12 and 3.13 only

**Files:**
- Create: `.github/workflows/coverage.yml`
- Create: `.codecov.yml`

- [ ] **Step 1: Write `.github/workflows/coverage.yml`**

```yaml
name: Codecov

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

permissions: read-all

jobs:
  upload-coverage:
    name: Upload Coverage to Codecov
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: read
      actions: read

    steps:
      - name: Harden the runner (Audit outbound calls)
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - name: Download coverage artifacts (Python 3.12)
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: test-results-3.12
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}
          path: coverage-3.12
        continue-on-error: true

      - name: Download coverage artifacts (Python 3.13)
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: test-results-3.13
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}
          path: coverage-3.13
        continue-on-error: true

      - name: Upload coverage to Codecov (Python 3.12)
        if: hashFiles('coverage-3.12/coverage.xml') != ''
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2  # v6.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: ${{ github.repository }}
          file: coverage-3.12/coverage.xml
          fail_ci_if_error: false
          flags: python-3.12
          name: family-office-portal Coverage (Python 3.12)
          commit_parent: ${{ github.event.workflow_run.head_sha }}
        continue-on-error: true

      - name: Upload coverage to Codecov (Python 3.13)
        if: hashFiles('coverage-3.13/coverage.xml') != ''
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2  # v6.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: ${{ github.repository }}
          file: coverage-3.13/coverage.xml
          fail_ci_if_error: false
          flags: python-3.13
          name: family-office-portal Coverage (Python 3.13)
          commit_parent: ${{ github.event.workflow_run.head_sha }}
        continue-on-error: true

      - name: Coverage upload summary
        if: always()
        env:
          GH_REPOSITORY: ${{ github.repository }}
        run: |
          echo "## Codecov Upload Summary" >> $GITHUB_STEP_SUMMARY
          for version in 3.12 3.13; do
            if [ -f "coverage-${version}/coverage.xml" ]; then
              echo "- Python ${version}: Coverage uploaded" >> $GITHUB_STEP_SUMMARY
            else
              echo "- Python ${version}: No coverage report found" >> $GITHUB_STEP_SUMMARY
            fi
          done
```

- [ ] **Step 2: Write `.codecov.yml`**

```yaml
coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 1%

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false

flags:
  python-3.12:
    paths:
      - app/
  python-3.13:
    paths:
      - app/
```

- [ ] **Step 3: Verify**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/coverage.yml'))" && echo "Valid YAML"
python3 -c "import yaml; yaml.safe_load(open('.codecov.yml'))" && echo "Valid YAML"
# Expected: Valid YAML (twice)
```

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/coverage.yml .codecov.yml
git commit -m "ci: add Codecov coverage upload workflow (CI-018)"
```

---

### Task 17: Create `.github/workflows/scorecard.yml`

**Findings resolved:** CI-019 (OpenSSF Scorecard workflow required)

**Source template:** `/home/byron/dev/.github/workflow-templates/python-scorecard.yml`

**No adaptations needed.** The template is already a clean reusable caller with no project-specific variables.

**Files:**
- Create: `.github/workflows/scorecard.yml`

- [ ] **Step 1: Write `.github/workflows/scorecard.yml`**

```yaml
name: OpenSSF Scorecard

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * 0'  # Weekly Sunday 00:00 UTC

permissions:
  security-events: write
  id-token: write
  contents: read
  actions: read

jobs:
  scorecard:
    name: Scorecard Analysis
    uses: ByronWilliamsCPA/.github/.github/workflows/python-scorecard.yml@main
    with:
      publish-results: true
      upload-sarif: true
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
```

- [ ] **Step 2: Verify**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/scorecard.yml'))" && echo "Valid YAML"
# Expected: Valid YAML
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/scorecard.yml
git commit -m "ci: add OpenSSF Scorecard workflow (CI-019)"
```

---

### Task 18: Create `.github/workflows/python-compatibility.yml`

**Findings resolved:** CI-020 (compatibility matrix workflow required)

**Source template:** `/home/byron/dev/.github/workflow-templates/python-compatibility.yml`

**Adaptations from template:**
- Python versions: `["3.12", "3.13"]` only

**Files:**
- Create: `.github/workflows/python-compatibility.yml`

- [ ] **Step 1: Write `.github/workflows/python-compatibility.yml`**

```yaml
name: Python Compatibility

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  compatibility:
    name: Compatibility Matrix
    uses: ByronWilliamsCPA/.github/.github/workflows/python-compatibility.yml@main
    with:
      python-versions: '["3.12", "3.13"]'
      operating-systems: '["ubuntu-latest"]'
      include-windows: false
      include-macos: false
      source-directory: 'app'
      test-command: 'pytest -v'
      coverage-report: true
      fail-fast: false
```

- [ ] **Step 2: Verify**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/python-compatibility.yml'))" && echo "Valid YAML"
# Expected: Valid YAML
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/python-compatibility.yml
git commit -m "ci: add Python compatibility matrix workflow (CI-020)"
```

---

### Task 19: Create `.github/workflows/docs.yml`

**Findings resolved:** CI-021 (documentation workflow required)

**Source template:** `/home/byron/dev/.github/workflow-templates/python-docs.yml`

**Adaptations from template:**
- Replace `{{ cookiecutter.docstring_coverage_target }}` with `70`
- Change `src/` references to `app/`
- The template has two `name: Documentation` jobs (a full version and a disabled stub); keep the full version

**Files:**
- Create: `.github/workflows/docs.yml`

- [ ] **Step 1: Write `.github/workflows/docs.yml`**

```yaml
name: Documentation

on:
  pull_request:
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "app/**"
      - ".github/workflows/docs.yml"
  push:
    branches: [main, develop]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "app/**"

permissions:
  contents: read
  pull-requests: write

jobs:
  docstrings:
    name: Docstring Quality Gates
    runs-on: ubuntu-latest
    steps:
      - name: Harden the runner
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40  # v2.19.0
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: "3.12"

      - name: Install UV
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install dependencies
        run: uv sync --no-interaction --all-extras

      - name: Check docstring style
        run: uv run ruff check app/
        continue-on-error: true

      - name: Check docstring coverage (target 70%)
        run: uv run interrogate -v app/
        continue-on-error: true
```

- [ ] **Step 2: Verify**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docs.yml'))" && echo "Valid YAML"
grep -c "cookiecutter" .github/workflows/docs.yml
# Expected: Valid YAML
# Expected: 0
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/docs.yml
git commit -m "ci: add documentation workflow (CI-021)"
```

---

### Task 20: Create `.github/workflows/reuse.yml`

**Findings resolved:** CI-022 (REUSE FSFE compliance workflow required per org standard)

**Source template:** `/home/byron/dev/.github/workflow-templates/python-reuse.yml`

**No adaptations needed.** The template is already a clean reusable caller.

**Files:**
- Create: `.github/workflows/reuse.yml`

- [ ] **Step 1: Write `.github/workflows/reuse.yml`**

```yaml
name: REUSE Compliance

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  reuse:
    name: REUSE Compliance Check
    uses: ByronWilliamsCPA/.github/.github/workflows/python-reuse.yml@main
    with:
      generate-spdx: true
      fail-on-missing: true
```

- [ ] **Step 2: Verify**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/reuse.yml'))" && echo "Valid YAML"
# Expected: Valid YAML
```

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/reuse.yml
git commit -m "ci: add REUSE FSFE compliance workflow (CI-022)"
```

---

### Task 21: Create `.github/copilot-instructions.md`

**Findings resolved:** CI-023 (Copilot instructions file required per org standard)

**Files:**
- Create: `.github/copilot-instructions.md`

- [ ] **Step 1: Write `.github/copilot-instructions.md`**

```markdown
# GitHub Copilot Instructions

This file provides context for GitHub Copilot when working in this repository.

## Project

`family-office-portal` is a FastAPI web application that aggregates financial
and entity data from internal backend services for a single authenticated family.
It runs behind Cloudflare Zero Trust and is read-only from the user's perspective.

## Key conventions

- **Source root**: `app/` (not `src/`)
- **Python**: 3.12+ syntax; use `X | Y` union types (not `Optional[X]`)
- **Package manager**: uv; lock file is `uv.lock`
- **Type checker**: basedpyright strict (not mypy)
- **Linter**: ruff (88 chars, PyStrict-aligned rules)
- **Tests**: pytest with pytest-asyncio; 80% coverage minimum
- **Commits**: conventional commits required (`feat:`, `fix:`, `docs:`, etc.)
- **Comments**: no em-dashes ever; one short line max per comment

## Do not do

- Do not create `.py` files outside `app/`
- Do not use `Optional[X]`; use `X | None`
- Do not add `# type: ignore` without a tracking reference
- Do not use `poetry` or bare `pip install`; use `uv sync`
- Do not bypass pre-commit hooks with `--no-verify`
```

- [ ] **Step 2: Verify**

```bash
grep -c "Source root" .github/copilot-instructions.md
# Expected: 1
```

- [ ] **Step 3: Commit**

```bash
git add .github/copilot-instructions.md
git commit -m "docs: add .github/copilot-instructions.md (CI-023)"
```

---

## Phase C: GitHub Platform Settings

Phase C requires browser UI or gh CLI with admin/owner permissions. Tasks are
documented as checklists; they cannot be automated within the working tree.

---

### Task 22: Enable GitHub Private Vulnerability Reporting

**Findings resolved:** OSSF-002 (PVR must be enabled so the SECURITY.md link works)

- [ ] **Step 1: Enable via gh CLI**

```bash
gh api \
  --method PUT \
  repos/ByronWilliamsCPA/family-office-portal/private-vulnerability-reporting \
  -H "Accept: application/vnd.github+json"
```

- [ ] **Step 2: Verify PVR is enabled**

```bash
gh api repos/ByronWilliamsCPA/family-office-portal/private-vulnerability-reporting \
  --jq '.enabled'
# Expected: true
```

- [ ] **Step 3: Test the advisory link**

Open a browser and navigate to:
`https://github.com/ByronWilliamsCPA/family-office-portal/security/advisories/new`

Expected: The form loads (not a 404). You do not need to submit it.

---

### Task 23: Configure branch protection on `main`

**Findings resolved:** CI-* (required status checks enforce CI gate; prevents merge without passing CI)

The four required status check names (must match CI job names exactly):
- `CI Gate`
- `Security Gate Validation`
- `Dependency & Standards Validation`
- `Scorecard Analysis`

- [ ] **Step 1: Apply branch protection via gh CLI**

```bash
gh api \
  --method PUT \
  repos/ByronWilliamsCPA/family-office-portal/branches/main/protection \
  -H "Accept: application/vnd.github+json" \
  --field required_status_checks='{"strict":true,"contexts":["CI Gate","Security Gate Validation","Dependency & Standards Validation","Scorecard Analysis"]}' \
  --field enforce_admins=true \
  --field required_pull_request_reviews='{"required_approving_review_count":1,"dismiss_stale_reviews":true}' \
  --field restrictions=null
```

- [ ] **Step 2: Verify**

```bash
gh api repos/ByronWilliamsCPA/family-office-portal/branches/main/protection \
  --jq '.required_status_checks.contexts'
# Expected: ["CI Gate", "Security Gate Validation", "Dependency & Standards Validation", "Scorecard Analysis"]
```

---

### Task 24: Enable Organization 2FA requirement

**Findings resolved:** OSSF-007 (org 2FA enforcement required for OpenSSF scorecard)

This cannot be done via API; it requires GitHub org settings UI.

- [ ] **Step 1: Navigate to org security settings**

Open: `https://github.com/organizations/ByronWilliamsCPA/settings/security`

- [ ] **Step 2: Enable 2FA requirement**

Under "Two-factor authentication", click **"Require two-factor authentication"** and confirm.

- [ ] **Step 3: Verify via gh CLI after enabling**

```bash
gh api orgs/ByronWilliamsCPA --jq '.two_factor_requirement_enabled'
# Expected: true
```

---

### Task 25: File OpenSSF Best Practices Badge application

**Findings resolved:** OSSF-001 (badge application required for public repos)

- [ ] **Step 1: Submit badge application**

Navigate to: `https://www.bestpractices.dev/en/projects/new`

Fill in:
- Repository URL: `https://github.com/ByronWilliamsCPA/family-office-portal`
- Project name: `family-office-portal`

- [ ] **Step 2: Add badge to README.md**

After the application is submitted, the badge URL will be provided. Add it to `README.md`
in the badges section:

```markdown
[![OpenSSF Best Practices](https://www.bestpractices.dev/projects/<ID>/badge)](https://www.bestpractices.dev/projects/<ID>)
```

Replace `<ID>` with the numeric project ID from the bestpractices.dev URL.

- [ ] **Step 3: Commit the badge**

```bash
git add README.md
git commit -m "docs: add OpenSSF Best Practices badge to README (OSSF-001)"
```

---

## Self-Review

### Spec coverage

| Finding category | Tasks that resolve it |
|---|---|
| FOUND-004 CODEOWNERS location | Task 1 |
| FOUND-006 .gitignore compliance-reports | Task 2 |
| FOUND-007 authors field | Task 3 |
| FOUND-009 CHANGELOG.md | Task 6 |
| FOUND-012 architecture docs location | Task 10 |
| FOUND-013 SPDX in LICENSE | Task 7 |
| TOOL-007 darglint dev dep | Task 3 |
| PC-006 darglint hook | Task 4 |
| OSSF-001 OpenSSF badge | Task 25 |
| OSSF-002 GitHub PVR in SECURITY.md + enabled | Tasks 5, 22 |
| OSSF-007 org 2FA | Task 24 |
| CI-007 quality gate continue-on-error | Task 15 |
| CI-014 ci-gate job named "CI Gate" | Task 12 |
| CI-015 security-gate job | Task 13 |
| CI-016 PR validation job name | Task 14 |
| CI-017 SonarCloud | Task 15 |
| CI-018 Codecov | Task 16 |
| CI-019 Scorecard workflow | Task 17 |
| CI-020 Compatibility matrix | Task 18 |
| CI-021 Docs workflow | Task 19 |
| CI-022 REUSE workflow | Task 20 |
| CI-023 Copilot instructions | Task 21 |
| CLAUDE-006 AGENTS.md | Task 9 |
| Branch protection with 4 required checks | Task 23 |
| CVE tracking baseline | Task 8 |
| Renovate dependency automation | Task 11 |

### Placeholder scan

No TBD, TODO, "implement later", "fill in details", "add appropriate error handling",
"write tests for the above", or "similar to Task N" phrases are present.

### Type consistency

No typed interfaces are defined across tasks; this plan creates configuration and
workflow files only. Method names are not defined, so cross-task type consistency
does not apply.
